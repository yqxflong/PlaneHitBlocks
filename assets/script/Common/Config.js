let CONFIG = {
    INTERVAL_BULLETNORMAL : 0.06, //普通子弹间隔
    INTERVAL_BULLETSUPER : 0.02,  //超级子弹间隔
    DIS_BULLET : 10, //子弹移动距离(前后)
    DIS_PLANE_SPEED : 500, //飞机移动速度(左右)
    DIS_TRAVEL : 3, //飞机行驶距离(前后)
    DIS_BONUS : 5,  //bonus移动距离（前后）
    DIS_BLOCK : 3,  //blocks移动距离（前后）
    INTERVAL_GEN_BONUS : 5, //生成道具间隔
    INTERVAL_GEN_BLOCKS : 5, //生成blocks间隔
}

export default CONFIG;