let CONFIG = {
    INTERVAL_BULLETNORMAL : 0.1, //普通子弹间隔
    INTERVAL_BULLETSUPER : 0.05,  //超级子弹间隔
    DIS_BULLET : 10, //子弹移动距离(前后)
    DURING_BONUS_SUPERBULLET: 5, //超级子弹持续时间
    DIS_PLANE_SPEED : 600, //飞机移动速度(左右)
    DIS_TRAVEL : 3, //飞机行驶距离(前后)
    DIS_BONUS : 5,  //bonus移动距离（前后）
    DIS_BLOCK_SPEED : 200,  //blocks移动速度（前后）
    INTERVAL_GEN_BONUS : 5, //生成道具间隔
    INTERVAL_GEN_BLOCKS : 3, //生成blocks间隔   
}

export default CONFIG;